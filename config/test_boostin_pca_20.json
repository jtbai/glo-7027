{
    "trained_model_path" : "trained_models",
    "output_path" : "resultats",

    "model_to_train":[
        "boosting"
    ],

    "model_to_run":[
        "boosting"
    ],

    "trained_transformation_path": "trained_data_transformation",
    "data_transformation" : [
        {
            "name":"pca",
            "parameters": {
                "number_of_component": 20
            }
        }
    ]
}
